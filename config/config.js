const config = {
    // eslint-disable-next-line
    PORT: process.env.PORT || 8080,
    DB_LOCAL_CONNECTION_STRING: 'mongodb://localhost',
    DB_NAME: 'cryptoShitties',
};

module.exports = config;
